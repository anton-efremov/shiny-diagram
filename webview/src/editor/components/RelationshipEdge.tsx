import type { ReactElement } from "react";

// Stub — custom relationship edge component not yet implemented; canvas uses default RF edge.
export default function RelationshipEdge(): ReactElement {
  return <></>;
}
