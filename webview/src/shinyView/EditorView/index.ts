/**
 * @fileoverview Public command contract of the View layer
 * for root React component. Reexported for API normalization purposes:
 * to be reached via "../shinyView/EditorView"
 */

export { default as EditorView } from "./EditorView";
