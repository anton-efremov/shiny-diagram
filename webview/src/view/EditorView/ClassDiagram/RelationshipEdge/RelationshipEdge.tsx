import type { ReactElement } from "react";

/**
 * Placeholder for custom relationship edges while the canvas uses ReactFlow defaults.
 */
export default function RelationshipEdge(): ReactElement {
  return <></>;
}
